using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InputManager : MonoBehaviour
{
    //private RaycastHit hit;
    //private float maxDistance = 40f;

    //// Start is called before the first frame update
    //void Start()
    //{

    //}

    //// Update is called once per frame
    //void Update()
    //{
    //    Debug.DrawRay(transform.position, transform.up * maxDistance, Color.green);
    //    Debug.DrawRay(transform.position, transform.right * maxDistance, Color.green);
    //    Debug.DrawRay(transform.position, transform.right * -1 * maxDistance, Color.green);
    //    RayAll();
    //}

    //void RayAll()
    //{
    //    RaycastHit2D[] hits = Physics2D.RaycastAll(transform.position, transform.forward, maxDistance);

    //    for(int i = 0; i < hits.Length; i++)
    //    {
    //        Debug.Log(hits[i].transform.gameObject.name);
    //    }
    //}
}
